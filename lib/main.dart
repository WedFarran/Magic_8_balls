import 'package:flutter/material.dart';

void main() {
  runApp(const MagicBalls());
}

class MagicBalls extends StatelessWidget {
  const MagicBalls({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
